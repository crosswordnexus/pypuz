Don't forget to bump the version in pyproject.toml before making changes
To build: `poetry build`
To install locally (after build): `pip install dist/pypuz-VERSION-py3-none-any.whl`
To upload to pypi: `twine upload dist/*`
